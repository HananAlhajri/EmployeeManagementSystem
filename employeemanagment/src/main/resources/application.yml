spring:
  datasource:
    password: password
    url: jdbc:mysql://localhost:3306/department
    username: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format-sql: true

